name: Build Android APK
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
          pip install --upgrade pip
          pip install "cython<3.0.0" buildozer virtualenv

      - name: Configure and Build APK
        run: |
          # 1. Apply the NDK fix to prevent the standard crash
          sed -i 's/android.api = .*/android.api = 33/' buildozer.spec || true
          sed -i 's/#android.ndk = .*/android.ndk = 25b/' buildozer.spec || true
          
          # 2. Run the build. If it fails, print ONLY the last 100 lines!
          yes | buildozer android debug > build_log.txt 2>&1 || (echo "🚨 BUILD FAILED! Here is the actual error:" && tail -n 100 build_log.txt && exit 1)

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: AI-Shell-App
          path: bin/*.apk
